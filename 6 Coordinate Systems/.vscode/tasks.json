{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Build the project with make",
			"command": "make",
			
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": {
				"base": "$gcc"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"detail": "Makefile"
		}
	]
}